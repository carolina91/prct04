#!/usr/bin/python
#!encoding: UTF-8
 

a=float(raw_input('valor de a: '))
b=float(raw_input('valor de b: '))

if (a != 0):
  x= -b/a
  print 'solucion: ', x
else
print'No tiene solucion'

# pide dos enteros, luego realiza la operación, donde el valor a nunca puede ser 0.