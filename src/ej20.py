#!/usr/bin/python
#!encoding: UTF-8


x=-b/a

a=float(raw_input('valor de a: '))
b=float(raw_input('valor de b: '))

print'solucion: ', x

#El programa lee dos números y realiza el cociente entre ellos. Hace la operación antes de saber el valor de las variables, es decir, antes de pedirlas, por lo que esta mal.